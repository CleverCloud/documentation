name: Test CLI

on:
  push:
    branches:
      - ci-cd

jobs:
  install:
    runs-on: ubuntu-latest 
    steps:
      - id: install
        run: npm install -g clever-tools
      - id: command
        run: clever --version
